# 🚀 我的 Git & GitHub 极简操作指南

这份文档是我在配置 `某app` 和测试上传时沉淀的经验，防止以后忘记。 *(AI 协助生成)*

---

## 🛡️ 第一部分：安保检查 (非常重要)
在任何推送之前，必须确保隐私文件已被拦截。

### 1. 确认 .gitignore 生效
* **文件名**：必须是 `.gitignore`，不能有 `.txt` 后缀。
* **视觉标志**：在 VS Code 侧边栏中：
    * **灰色**：Git 已忽略（安全 ✅）。
    * **绿色**：新创建的文件，尚未跟踪。
    * **白色/亮色**：已跟踪的文件，有改动会变色。
* **强制刷新命令**：如果写了规则文件还没变灰，运行：
    ```bash
    git rm -r --cached .
    git add .
    ```
    *(这会刷新整个缓存，让所有该变灰的文件瞬间变灰。)*

---

## 🏗️ 第二部分：日常推送三部曲
这是最常用的“无脑”同步流程：

1. **点名 (Stage)**：把改动加入待发货清单
    ```bash
    git add .
    ```
2. **打包 (Commit)**：给这次改动贴个标签
    ```bash
    git commit -m "这里写你的更新说明"
    ```
3. **发射 (Push)**：推送到 GitHub
    ```bash
    git push
    ```

---

## 🛠️ 第三部分：常见报错与急救

### 1. 远程已经存在 (Already exists)
如果报错 `remote origin already exists`，说明地址已经绑过了。想换新地址用：
`git remote set-url origin [新链接]`

### 2. 线上有更新 (Rejected)
如果推送失败，提示 `fetch first`，说明 GitHub 线上有变动，先拉再推：
`git pull origin main --rebase`

### 3. 查看当前状态
如果不确定当前进展，随时输入：
`git status`

---

## 🔗 第四部分：如何提取 Raw 链接 (Gemini 调试用)
1. 登录 GitHub 网页，点击目标文件（如 `config.json`）。
2. 点击内容区域右上角的 **[Raw]** 按钮。
3. 复制浏览器地址栏里以 `https://raw.githubusercontent.com/...` 开头的链接发给大神。

---

## ⌨️ 第五部分：VS Code 常用快捷键 (常用快捷键)

| 快捷键 | 功能描述 | 场景 |
| :--- | :--- | :--- |
| **`Ctrl + Shift + P或者F1`** | **命令面板** | 所有的功能入口（如 `Add gitignore`, `Reload`） |
| **`Ctrl + \``** | **开关终端** | 快速调出输入 Git 命令的窗口 |
| **`Ctrl + Shift + O`** | **快速跳转大纲** | 在长代码或 README 里快速定位标题 |
| **`Ctrl + Shift + V`** | **预览 Markdown** | 实时查看 README 渲染后的精美效果 |
| **`Alt + Up/Down`** | **整行移动** | 快速调整代码行的上下顺序 |
| **`Ctrl + /`** | **快速注释** | 临时屏蔽某行配置代码 |
| **`Ctrl + F`** | **快速查找** | 在配置文件里搜关键词 |

---

### 💡 提示
* 修改完此文档后，记得执行 **“推送三部曲”** 将其同步到 GitHub。
* 如果 VS Code 侧边栏没看到“大纲”，请右键点击侧边栏空白处勾选 **“大纲”**。